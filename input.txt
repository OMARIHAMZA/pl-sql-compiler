int main(){
SELECT EMPLOYEES.EMPLOYEE_NAME, BRANCHES.BRANCH_PHONE FROM EMPLOYEES JOIN DEPARTMENTS ON EMPLOYEES.DEPARTMENT_ID == DEPARTMENTS.DEPARTMENT_ID JOIN BRANCHES ON DEPARTMENTS.BRANCH_ID == BRANCHES.BRANCH_ID ORDER BY EMPLOYEES.EMPLOYEE_NAME
}

void b(){
}