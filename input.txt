int main(){

SELECT MAX(EMPLOYEES.SALARY) OVER (PARTITION BY EMPLOYEES.DEPARTMENT_ID),
       MIN(EMPLOYEES.SALARY) OVER (PARTITION BY EMPLOYEES.DEPARTMENT_ID),
       EMPLOYEES.EMPLOYEE_NAME FROM EMPLOYEES;

}