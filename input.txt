int main(){
SELECT E.DEPARTMENT_ID, E.EMPLOYEE_NAME, D.DEPARTMENT_NAME FROM (SELECT * FROM EMPLOYEES) E JOIN (SELECT * FROM DEPARTMENTS) D ON E.DEPARTMENT_ID == D.DEPARTMENT_ID WHERE D.DEPARTMENT_NAME != "Support";
}