int main(){

    select EMPLOYEES.EMPLOYEE_NAME
    ,EMPLOYEES.DEPARTMENT_ID
    ,AVG(EMPLOYEES.SALARY) OVER(PARTITION BY EMPLOYEES.DEPARTMENT_ID),
    MIN(EMPLOYEES.SALARY) OVER()
    FROM EMPLOYEES;
}