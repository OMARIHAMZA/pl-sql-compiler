int main(){

SELECT * FROM EMPLOYEES
JOIN DEPARTMENTS ON EMPLOYEES.DEPARTMENT_ID == DEPARTMENTS.DEPARTMENT_ID
INNER JOIN BRANCHES ON DEPARTMENTS.BRANCH_ID == BRANCHES.BRANCH_ID
WHERE BRANCHES.BRANCH_LOCATION == "Damascus";

}